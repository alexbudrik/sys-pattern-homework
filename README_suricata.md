# Задание 1: 

## Проведите разведку системы и определите, какие сетевые службы запущены на защищаемой системе:

* sudo nmap -sA < ip-адрес >

* sudo nmap -sT < ip-адрес >

* sudo nmap -sS < ip-адрес >

* sudo nmap -sV < ip-адрес >

# Выполнение

## Suricata: обнаружила подозрительную сетевую активность и сформировала события безопасности, подтверждая работу системы обнаружения вторжений.

## Fail2Ban: зарегистрировал неудачные попытки входа по SSH. После превышения порога maxretry система автоматически блокирует IP-адрес атакующего, защищая сервер от атак перебора паролей.

![suricata](https://github.com/alexbudrik/sys-pattern-homework/blob/main/screenshots/suricana%201.png)
![suricata](https://github.com/alexbudrik/sys-pattern-homework/blob/main/screenshots/suricana%202.png)
![suricata](https://github.com/alexbudrik/sys-pattern-homework/blob/main/screenshots/suricana%203.png)
![suricata](https://github.com/alexbudrik/sys-pattern-homework/blob/main/screenshots/suricana%204.png)
![suricata](https://github.com/alexbudrik/sys-pattern-homework/blob/main/screenshots/suricana%205.png)


# Задание 2

## Проведите атаку на подбор пароля для службы SSH:

* hydra -L users.txt -P pass.txt < ip-адрес > ssh

### Настройка hydra:
создайте два файла: users.txt и pass.txt;
в каждой строчке первого файла должны быть имена пользователей, второго — пароли. В нашем случае это могут быть случайные строки, но ради эксперимента можете добавить имя и пароль существующего пользователя.

### Включение защиты SSH для Fail2Ban:
открыть файл /etc/fail2ban/jail.conf,
найти секцию ssh,
установить enabled в true.

В качестве ответа пришлите события, которые попали в логи Suricata и Fail2Ban, прокомментируйте результат.

# Выполнение

## Конфигурация стенда

* Защищаемая система:
Debian 13
IP-адрес: 192.168.1.4
Установлены:
Suricata
Fail2Ban
OpenSSH Server

* Атакующая система:
Debian 13
IP-адрес: 192.168.1.5
Установлены:
Nmap
THC-Hydra

## Выполнение атаки

Для подбора пароля к службе SSH были подготовлены два файла:
```
    users.txt
    root
    vboxuser
    admin

    pass.txt
    123456
    password
    admin
    debian
    qwerty
```
Атака выполнялась командой:
```
    hydra -L users.txt -P pass.txt ssh://192.168.1.4
```
Результаты работы Suricata

Во время выполнения атаки Suricata анализировала сетевой трафик между атакующей и защищаемой машинами и зарегистрировала события безопасности.

Пример события:
```
    Source IP: 192.168.1.5
    Destination IP: 192.168.1.4
    Event type: alert
```
# Комментарий

* Suricata успешно обнаружила подозрительную сетевую активность, связанную с подключениями к службе SSH. Все события были записаны в журнал eve.json, что подтверждает корректную работу системы обнаружения вторжений.
* Результаты работы Fail2Ban
После выполнения атаки Fail2Ban зарегистрировал многочисленные неудачные попытки входа по SSH.
* Результат команды:
```
    Status for the jail: sshd

    Currently failed: 1
    Total failed: 19

    Currently banned: 1
    Total banned: 1

    Banned IP list:
    192.168.1.5
```
# Комментарий

* Fail2Ban успешно обнаружил многократные неудачные попытки аутентификации по SSH. После превышения установленного порога (maxretry) IP-адрес атакующей машины 192.168.1.5 был автоматически заблокирован.

* Это подтверждает корректную работу механизма защиты от атак перебора паролей.

## Вывод

В ходе выполнения лабораторной работы была успешно проведена атака на подбор пароля к службе SSH с использованием программы Hydra.
Система Suricata обнаружила подозрительную сетевую активность и зарегистрировала события безопасности в журнале.
Система Fail2Ban зафиксировала многочисленные неудачные попытки входа по SSH и автоматически заблокировала IP-адрес атакующей машины (192.168.1.5).
Таким образом, обе системы защиты продемонстрировали корректную работу:
Suricata обеспечивает обнаружение сетевых атак и подозрительной активности.
Fail2Ban обеспечивает защиту служб от атак перебора паролей путём автоматической блокировки источника атаки.

